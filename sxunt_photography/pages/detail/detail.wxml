<view class="detail-container">
  <image src="{{image.url}}" mode="widthFix" class="detail-img" />
  <view class="detail-title">{{image.title || image.desc}}</view>
  <view class="detail-desc">灵感提示：{{image.inspiration || '用不同的角度观察世界，发现独特美感'}}</view>
</view>