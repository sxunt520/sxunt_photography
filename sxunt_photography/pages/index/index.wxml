<view class="container">
  <!-- <t-button theme="primary">按钮</t-button> -->

  <!-- 顶部轮播图 -->
  <!-- <swiper indicator-dots autoplay circular>
    <block wx:for="{{carouselImages}}" wx:key="index">
      <swiper-item>
        <image src="{{item}}" mode="aspectFill" class="carousel-img" />
      </swiper-item>
    </block>
  </swiper> -->

   <!-- adv_banner -->
   <swiper indicator-dots autoplay circular>
    <block wx:for="{{advBanner}}" wx:key="id">
      <swiper-item bindtap="jumpurl" data-jumpurl="{{item.jumpurl}}">
        <image src="{{item.advimg}}" mode="aspectFill" class="carousel-img" />
      </swiper-item>
    </block>
  </swiper>

  <!-- 跑马灯公告 -->
  <view class="marquee">
    <text>{{announcement}}</text>
  </view>

  <!-- swiperList3 -->
  <swiper  class="swiper-container" circular autoplay layout-type="transformer" transformer-type="deepthPage" previous-margin="60px"
			 next-margin="60px">
    <block wx:for="{{swiperList3}}" wx:key="id">
      <swiper-item bindtap="jumpurl" data-jumpurl="{{item.jumpurl}}">
        <image src="{{item.advimg}}" mode="aspectFill" class="carousel-img" />
      </swiper-item>
    </block>
  </swiper>

  <!-- 卡片式（cards）轮播图 -->
  <!-- <view class="card-theme">
  <t-swiper
    current="{{current}}"
    autoplay="{{autoplay}}"
    duration="{{duration}}"
    interval="{{interval}}"
    bindchange="onChange"
    navigation="{{ { type: 'dots' } }}"
    list="{{swiperList2}}"
    image-props="{{ { shape: 'round' } }}"
    previousMargin="34px"
    nextMargin="34px"
    t-class-nav="card-theme-nav"
    height="400"
    bind:click="navToActivityDetail"
  />
</view> -->

<view class="recommend-title">推荐图片</view>
  <view class="recommend-list">
    <block wx:for="{{EdifyList}}" wx:key="data-id">
      <view class="recommend-item" bindtap="goEdifyDetail" data-id="{{item.id}}">
        <image src="{{item.px260Pic}}" mode="aspectFill" />
        <view class="img-title">{{item.title}}</view>
      </view>
    </block>
  </view>

  <!-- 推荐图片列表 -->
  <view class="recommend-title">推荐图片</view>
  <view class="recommend-list">
    <block wx:for="{{recommendImages}}" wx:key="index">
      <view class="recommend-item" bindtap="goDetail" data-id="{{item.id}}">
        <image src="{{item.url}}" mode="aspectFill" />
        <view class="img-title">{{item.title}}</view>
      </view>
    </block>
  </view>
  
<!--  -->
 <!-- <swiper>
    <block wx:for="{{storys}}" wx:key="id">
      <swiper-item>
        <image src="{{item.cover_url}}" />
      </swiper-item>
    </block>
  </swiper> -->

  <!-- apifm-wxapi 后台生成的banner -->
  <!-- <swiper>
    <block wx:for="{{banners}}" wx:key="id">
      <swiper-item>
        <image src="{{item.picUrl}}" />
      </swiper-item>
    </block>
  </swiper>

<button type="warn" bindtap="cmsArticles"> 获取所有文章 </button>
<button type="warn" bindtap="cmsArticleDetail"> 文章详情 </button> -->




</view>